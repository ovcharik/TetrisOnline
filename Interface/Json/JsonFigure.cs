using System;
using Newtonsoft.Json;

namespace Interface.Json
{
    namespace Figure
    {
        public enum MoveAction
        {
            ToLeft,
            ToRight,
            ToDown,
            ToBottom
        }

        public enum Type
        {
            I, J, L, O, S, T, Z
        }
    }
}
